spring:
  application:
    name: order-service   # must match config-repo file
  profiles:
    active: dev                  # 👈 environment (dev / test / prod)
  config:
    import: configserver:http://config-container:8888
